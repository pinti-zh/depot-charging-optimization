#!/bin/bash

# Loop through all files in the data/processed directory
for file in data/processed/*; do
    # Check if it is a file (not a subdirectory)
    if [[ -f "$file" ]]; then
        echo "Processing: $file"
        python main.py --data "$file"
    fi
done